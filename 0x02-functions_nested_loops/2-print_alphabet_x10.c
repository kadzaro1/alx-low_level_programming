#include "main.h"

/**
 * print_alphabet - prints alphabet
 *
 * _putchar: prints to the output stream
 */
void print_alphabet_10x(void)
{
int i = 0;
if (i <= 10)
{
i++
int  ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
_putchar(ch);
}
_putchar('\n');
}
}
