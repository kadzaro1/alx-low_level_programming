#include "main.h"
/**
 * _islower - checks for lowercase character
 *
 * @c: a character parameter passed into function _islower
 *
 * Return: 1 if true and 0 if otherwise
 */
int _islower(int c)
{
return (c >= 'a' && <= 'z');
}
